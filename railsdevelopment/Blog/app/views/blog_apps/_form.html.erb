<%= form_for(@blog_app) do |f| %>
  <% if @blog_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_app.errors.count, "error") %> prohibited this blog_app from being saved:</h2>

      <ul>
      <% @blog_app.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :index %><br>
    <%= f.text_field :index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
